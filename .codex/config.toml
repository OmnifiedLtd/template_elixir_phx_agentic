# Project-scoped MCP configuration. Requires this project to be trusted.

[mcp_servers.tidewave]
url = "http://localhost:4000/tidewave/mcp"

[mcp_servers.playwright]
command = "npx"
args = ["-y", "@playwright/mcp@latest"]

[mcp_servers.fly]
# Run: fly mcp server --stream --bind-addr 127.0.0.1 --port 8080
url = "http://127.0.0.1:8080"
